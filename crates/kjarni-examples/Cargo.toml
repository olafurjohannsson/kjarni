[package]
name = "kjarni-examples"
version.workspace = true
edition = "2021"
description = "Examples for the Kjarni inference engine"
publish = false

[dependencies]
kjarni = { workspace = true }
anyhow = "1.0"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }