<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="../kjarni/Native.cs" />
  </ItemGroup>

  <Target Name="CopyKjarniFFINativeLib" AfterTargets="Build">
  <ItemGroup>
    <NativeLib Include="../../../../../target/release/libkjarni_ffi.so" />
  </ItemGroup>
  <Copy SourceFiles="@(NativeLib)" DestinationFolder="$(OutDir)" SkipUnchangedFiles="true" />
</Target>
</Project>
